# Solana Verifier

Deploy the program in `packages/solana-program`. The program expects a proof blob passed as instruction data and validates it by checking a prefix `multer-proof-`.

Use the Solana CLI or Anchor-free deployment to upload the compiled BPF program.
